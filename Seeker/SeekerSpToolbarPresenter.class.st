"
A presenter that uses a specific toolbar morph instead of the regular one. 
"
Class {
	#name : #SeekerSpToolbarPresenter,
	#superclass : #SpToolbarPresenter,
	#category : #'Seeker-UI'
}

{ #category : #specs }
SeekerSpToolbarPresenter class >> adapterName [

	^ #SeekerToolbarAdapter
]
